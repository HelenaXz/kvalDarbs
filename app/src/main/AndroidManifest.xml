<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hz.kvalifdarbs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".UserSelectActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddAdminActivity"
            android:label="Add Administrator" />
        <activity android:name=".AddPatientActivity"
            android:label="Add Patient" />
        <activity android:name=".AddDoctorActivity"
            android:label="Add Doctor"/>
        <activity android:name=".DoctorViewPatientActivity"
            android:label="Patient" />
        <activity android:name=".AllPatientListActivity"
            android:label="All patients" />
        <activity android:name=".AdminMainActivity"
            android:label="Hello"/>
        <activity android:name=".LoginActivity"
            android:label="Login" />
        <activity android:name=".AdminViewPatientActivity"
            android:label="Patient"/>
        <activity android:name=".AdminViewDoctorActivity"
            android:label="Doctor"/>
        <activity android:name=".AvaiableDoctorListActivity"
            android:label="Available Doctors  "/>
        <activity android:name=".DoctorMainActivity"
            android:label="Hello"/>
        <activity android:name=".PatientMainActivity"
            android:label="Hello"/>
        <activity android:name=".DoctorPatientListActivity"
            android:label="My Patients"/>
        <activity android:name=".AllDoctorListActivity"
            android:label="All Doctors"/>
        <activity android:name=".ConnectDeviceActivity" />
    </application>

</manifest>